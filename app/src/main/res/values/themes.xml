<resources>
    <!--
        AppCompat, not the platform theme: the Cast route chooser and controller are
        AppCompatDialogs, and they read alertDialogTheme and the AppCompat colour attributes
        from whichever activity opened them. Compose paints everything else.
    -->
    <style name="Theme.Kaeru" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/kaeru_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
    <style name="Theme.Kaeru.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/kaeru_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Kaeru</item>
    </style>
</resources>
